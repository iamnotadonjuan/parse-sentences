{
  "name": "code-challenge-juan-camargo",
  "version": "1.0.0",
  "description": "it's a tiny algorithm for a code challenge",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JCamargo",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.2"
  }
}
